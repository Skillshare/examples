# Migration `watch-20191014091750`

This migration has been generated by Luis Aguilar at 10/14/2019, 9:17:50 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "lift"."Class" (
  "description" TEXT    ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "title" TEXT NOT NULL DEFAULT ''  
);

CREATE TABLE "lift"."ClassLesson" (
  "class" INTEGER    REFERENCES "Class"(id) ON DELETE SET NULL,
  "description" TEXT    ,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT ,
  "title" TEXT NOT NULL DEFAULT ''  
);

CREATE UNIQUE INDEX "lift"."Class.title" ON "Class"("title")

CREATE UNIQUE INDEX "lift"."ClassLesson.title" ON "ClassLesson"("title")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20191014091750
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,22 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  output   = "../node_modules/@generated/photon"
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+model Class {
+  id            Int     @id
+  title         String  @unique
+  description   String?
+  lessons       ClassLesson[]
+}
+
+model ClassLesson {
+  id            Int      @id
+  title         String   @unique
+  description   String?
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191014091750)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191014091750'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
