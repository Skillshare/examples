# Migration `watch-20191014084231`

This migration has been generated by Luis Aguilar at 10/14/2019, 8:42:31 AM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
DROP TABLE "lift"."Class_lessons";

CREATE TABLE "lift"."ClassLesson" (
  "class" INTEGER    REFERENCES "Class"(id) ON DELETE SET NULL,
  "id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT 
);
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration watch-20191014082205..watch-20191014084231
--- datamodel.dml
+++ datamodel.dml
@@ -11,6 +11,10 @@
 model Class {
   id            Int  @id
   title         String  @unique
   description   String?
-  lessons       Int[]
+  lessons       ClassLesson[]
+}
+
+model ClassLesson {
+  id  Int  @id
 }
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20191014084231)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20191014084231'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
